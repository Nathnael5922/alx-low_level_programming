/**
* is a program that prints latters
* in lowercase
* */

#include <stdio.h>
/**
 *  * main - Prints the alphabet in lowercase.
 *   *
 *    * Return: Always 0.
 *     */

int main(void)

	char latter;

	for (latter = 'a'; latter <= 'z' latter ++);
		putchar(latter);

	putchar("\n");
	
	return (0);
}	
